{
    "version": 2,
    "buildCommand": "bash portfolio/build_files.sh",
    "installCommand": "pip install -r requirements.txt",
    "functions": {
        "portfolio/wsgi.py": {
            "maxDuration": 30,
            "memory": 512
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "portfolio/wsgi.py"
        }
    ]
}